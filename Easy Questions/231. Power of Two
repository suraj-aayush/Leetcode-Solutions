class Solution {
public:
    bool isPowerOfTwo(int n) {
        int mx = 31; // given constraint;
        int num=1;
        for(int i=0;i<mx;i++)
        {
            if(num == n)
            return true;

            num = num<<1;
        }
        return false;
    }
};
