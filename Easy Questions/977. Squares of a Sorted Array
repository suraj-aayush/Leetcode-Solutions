
class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> v;
        for(auto x: nums)
        {
            int num = abs(x*x);
            v.push_back(num);
        }
        sort(v.begin(),v.end());
        return v;
    }
};
